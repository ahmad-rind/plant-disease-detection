apiVersion: v1
kind: Service
metadata:
  name: plant-disease-service
  labels:
    app: plant-disease-api
spec:
  type: NodePort
  selector:
    app: plant-disease-api
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8000
    nodePort: 30080
